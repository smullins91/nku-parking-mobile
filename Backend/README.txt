
Setup:
1) Download and install NodeJS from http://nodejs.org/download/
2) Open a command prompt in the location of the project.
3) Run the following command: npm install express mysql bcryptjs

Running on Windows:
1) Use the command: node server.js

Running on Linux:
1) Use the command: nodejs server.js